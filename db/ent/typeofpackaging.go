// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"main/db/ent/typeofpackaging"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// TypeOfPackaging is the model entity for the TypeOfPackaging schema.
type TypeOfPackaging struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TypeOfPackagingQuery when eager-loading is set.
	Edges        TypeOfPackagingEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TypeOfPackagingEdges holds the relations/edges for other nodes in the graph.
type TypeOfPackagingEdges struct {
	// TypeOfPackaging holds the value of the type_of_packaging edge.
	TypeOfPackaging []*Product `json:"type_of_packaging,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TypeOfPackagingOrErr returns the TypeOfPackaging value or an error if the edge
// was not loaded in eager-loading.
func (e TypeOfPackagingEdges) TypeOfPackagingOrErr() ([]*Product, error) {
	if e.loadedTypes[0] {
		return e.TypeOfPackaging, nil
	}
	return nil, &NotLoadedError{edge: "type_of_packaging"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TypeOfPackaging) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case typeofpackaging.FieldID:
			values[i] = new(sql.NullInt64)
		case typeofpackaging.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TypeOfPackaging fields.
func (top *TypeOfPackaging) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case typeofpackaging.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			top.ID = int(value.Int64)
		case typeofpackaging.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				top.Name = value.String
			}
		default:
			top.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TypeOfPackaging.
// This includes values selected through modifiers, order, etc.
func (top *TypeOfPackaging) Value(name string) (ent.Value, error) {
	return top.selectValues.Get(name)
}

// QueryTypeOfPackaging queries the "type_of_packaging" edge of the TypeOfPackaging entity.
func (top *TypeOfPackaging) QueryTypeOfPackaging() *ProductQuery {
	return NewTypeOfPackagingClient(top.config).QueryTypeOfPackaging(top)
}

// Update returns a builder for updating this TypeOfPackaging.
// Note that you need to call TypeOfPackaging.Unwrap() before calling this method if this TypeOfPackaging
// was returned from a transaction, and the transaction was committed or rolled back.
func (top *TypeOfPackaging) Update() *TypeOfPackagingUpdateOne {
	return NewTypeOfPackagingClient(top.config).UpdateOne(top)
}

// Unwrap unwraps the TypeOfPackaging entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (top *TypeOfPackaging) Unwrap() *TypeOfPackaging {
	_tx, ok := top.config.driver.(*txDriver)
	if !ok {
		panic("ent: TypeOfPackaging is not a transactional entity")
	}
	top.config.driver = _tx.drv
	return top
}

// String implements the fmt.Stringer.
func (top *TypeOfPackaging) String() string {
	var builder strings.Builder
	builder.WriteString("TypeOfPackaging(")
	builder.WriteString(fmt.Sprintf("id=%v, ", top.ID))
	builder.WriteString("name=")
	builder.WriteString(top.Name)
	builder.WriteByte(')')
	return builder.String()
}

// TypeOfPackagings is a parsable slice of TypeOfPackaging.
type TypeOfPackagings []*TypeOfPackaging
